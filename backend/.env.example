# Copy to backend/.env and fill in. backend/.env is gitignored — never commit real keys.

# Tavily is used ONLY at authoring time (document enrichment), never on the scan
# path. See docs/backend-plan.md "Document enrichment (Tavily, authoring-side)".
TAVILY_API_KEY=your-tavily-key-here
